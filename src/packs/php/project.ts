/** Read-only, bounded PHP source loading for first-party native analyzers. */

import { constants, type BigIntStats } from "node:fs";
import { lstat, open, readdir, type FileHandle } from "node:fs/promises";
import { basename, dirname, extname, join, parse, relative, resolve } from "node:path";

export const PHP_MAX_SOURCE_BYTES = 2 * 1024 * 1024;
export const PHP_MAX_SOURCE_FILES = 10_000;
export const PHP_MAX_TOTAL_BYTES = 64 * 1024 * 1024;
export const PHP_MAX_DEPTH = 32;
export const PHP_MAX_DISCOVERY_ENTRIES = 50_000;

const MAX_LIMITATIONS = 24;
const IGNORED_DIRS = new Set([
  ".cache", ".git", ".hg", ".idea", ".svn", ".vscode", "build", "cache",
  "coverage", "dist", "generated", "node_modules", "out", "storage", "target", "vendor",
]);
const NON_PRODUCTION_DIRS = new Set([
  "demo", "demos", "example", "examples", "fixture", "fixtures", "integration-test",
  "integration-tests", "sample", "samples", "test", "testdata", "tests",
]);

export interface PhpDocument {
  path: string;
  content: string;
}

export interface PhpProject {
  target: string;
  root: string;
  files: PhpDocument[];
  limitations?: string[];
}

export type PhpProjectInput = string | PhpProject | Promise<PhpProject>;

function sourcePath(path: string): boolean {
  return extname(path).toLowerCase() === ".php";
}

function nonProductionSourceName(name: string): boolean {
  const lower = name.toLowerCase();
  return lower.startsWith("test") || lower.endsWith("test.php") || lower.endsWith("tests.php") ||
    lower.endsWith("spec.php");
}

function generatedSource(name: string, content: string): boolean {
  const lower = name.toLowerCase();
  if (lower.endsWith(".generated.php") || lower.endsWith(".g.php")) return true;
  const header = content.split(/\r?\n/, 10).join("\n");
  return /(?:generated|autogenerated).{0,100}(?:do not edit|do not modify)/is.test(header);
}

function collector(): { add: (value: string) => void; finish: () => string[] } {
  const values = new Set<string>();
  let omitted = 0;
  return {
    add(value) {
      if (values.size < MAX_LIMITATIONS - 1) values.add(value);
      else omitted++;
    },
    finish() {
      return [...values, ...(omitted ? [`${omitted} additional PHP source limitations omitted.`] : [])].sort();
    },
  };
}

function normalized(path: string): string {
  return path.replace(/\\/g, "/");
}

function sameIdentity(left: BigIntStats, right: BigIntStats): boolean {
  return left.dev === right.dev && left.ino === right.ino;
}

function unchangedFile(before: BigIntStats, after: BigIntStats): boolean {
  return sameIdentity(before, after) && before.size === after.size &&
    before.mtimeNs === after.mtimeNs && before.ctimeNs === after.ctimeNs;
}

async function hasSymbolicLinkAncestor(absoluteTarget: string): Promise<boolean> {
  const filesystemRoot = parse(absoluteTarget).root;
  const ancestors: string[] = [];
  let current = dirname(absoluteTarget);
  while (current !== filesystemRoot) {
    ancestors.push(current);
    const parent = dirname(current);
    if (parent === current) break;
    current = parent;
  }
  for (const ancestor of ancestors.reverse()) {
    const metadata = await lstat(ancestor).catch(() => undefined);
    if (!metadata) break;
    if (metadata.isSymbolicLink()) return true;
  }
  return false;
}

async function readPhpFile(
  absolute: string,
  displayPath: string,
  remainingBytes: number,
): Promise<{ document?: PhpDocument; bytes?: number; limitation?: string; totalExceeded?: boolean }> {
  let handle: FileHandle | undefined;
  try {
    const before = await lstat(absolute, { bigint: true });
    if (before.isSymbolicLink()) return { limitation: `Skipped symbolic-link PHP source ${displayPath}.` };
    if (!before.isFile()) return {};
    if (before.size > BigInt(PHP_MAX_SOURCE_BYTES)) {
      return { limitation: `Skipped oversized PHP source ${displayPath} (limit: ${PHP_MAX_SOURCE_BYTES} bytes).` };
    }
    if (before.size > BigInt(remainingBytes)) return { bytes: Number(before.size), totalExceeded: true };
    const noFollow = typeof constants.O_NOFOLLOW === "number" ? constants.O_NOFOLLOW : 0;
    handle = await open(absolute, constants.O_RDONLY | noFollow);
    const opened = await handle.stat({ bigint: true });
    if (!opened.isFile() || !sameIdentity(before, opened)) {
      return { limitation: `Skipped changed PHP source ${displayPath}.` };
    }
    const size = Number(opened.size);
    const bytes = Buffer.alloc(size);
    let offset = 0;
    while (offset < size) {
      const { bytesRead } = await handle.read(bytes, offset, size - offset, offset);
      if (bytesRead === 0) return { limitation: `Skipped changed PHP source ${displayPath}.` };
      offset += bytesRead;
    }
    if (!unchangedFile(opened, await handle.stat({ bigint: true }))) {
      return { bytes: size, limitation: `Skipped changed PHP source ${displayPath}.` };
    }
    let content: string;
    try {
      content = new TextDecoder("utf-8", { fatal: true }).decode(bytes);
    } catch {
      return { bytes: size, limitation: `Skipped non-UTF-8 PHP source ${displayPath}.` };
    }
    if (generatedSource(basename(absolute), content)) return { bytes: size };
    return { document: { path: normalized(displayPath), content }, bytes: size };
  } catch {
    return { limitation: `Skipped unreadable PHP source ${displayPath}.` };
  } finally {
    await handle?.close().catch(() => undefined);
  }
}

async function loadUncached(target: string): Promise<PhpProject> {
  const absoluteTarget = resolve(target);
  const limitations = collector();
  if (await hasSymbolicLinkAncestor(absoluteTarget)) {
    return { target: absoluteTarget, root: sourcePath(absoluteTarget) ? dirname(absoluteTarget) : absoluteTarget, files: [], limitations: ["Skipped PHP source target because a symbolic-link ancestor would be followed; symbolic-link ancestors are never allowed."] };
  }
  const metadata = await lstat(absoluteTarget).catch(() => undefined);
  if (!metadata) return { target: absoluteTarget, root: absoluteTarget, files: [], limitations: ["PHP source target was unreadable."] };
  if (metadata.isSymbolicLink()) return { target: absoluteTarget, root: dirname(absoluteTarget), files: [], limitations: ["Skipped symbolic-link PHP source target; symbolic links are never followed."] };
  if (metadata.isFile()) {
    if (!sourcePath(absoluteTarget)) return { target: absoluteTarget, root: dirname(absoluteTarget), files: [] };
    const loaded = await readPhpFile(absoluteTarget, basename(absoluteTarget), PHP_MAX_TOTAL_BYTES);
    if (loaded.limitation) limitations.add(loaded.limitation);
    return { target: absoluteTarget, root: dirname(absoluteTarget), files: loaded.document ? [loaded.document] : [], limitations: limitations.finish() };
  }
  if (!metadata.isDirectory()) return { target: absoluteTarget, root: absoluteTarget, files: [] };

  const files: PhpDocument[] = [];
  let entriesInspected = 0;
  let totalBytes = 0;
  let stopped = false;
  async function walk(directory: string, depth: number): Promise<void> {
    if (stopped) return;
    if (depth > PHP_MAX_DEPTH) {
      limitations.add(`Skipped PHP source below ${normalized(relative(absoluteTarget, directory)) || "."} beyond the ${PHP_MAX_DEPTH}-level depth bound.`);
      return;
    }
    const entries = await readdir(directory, { withFileTypes: true }).catch(() => undefined);
    if (!entries) {
      limitations.add(`Skipped unreadable PHP source directory ${normalized(relative(absoluteTarget, directory)) || "."}.`);
      return;
    }
    entries.sort((left, right) => left.name.localeCompare(right.name));
    for (const entry of entries) {
      if (stopped) break;
      if (++entriesInspected > PHP_MAX_DISCOVERY_ENTRIES) {
        limitations.add(`Stopped PHP source discovery at the ${PHP_MAX_DISCOVERY_ENTRIES}-entry bound.`);
        stopped = true;
        break;
      }
      const absolute = join(directory, entry.name);
      const display = normalized(relative(absoluteTarget, absolute));
      if (entry.isSymbolicLink()) {
        limitations.add(`Skipped symbolic-link PHP source entry ${display}.`);
        continue;
      }
      if (entry.isDirectory()) {
        const lower = entry.name.toLowerCase();
        if (IGNORED_DIRS.has(lower) || NON_PRODUCTION_DIRS.has(lower)) continue;
        await walk(absolute, depth + 1);
        continue;
      }
      if (!entry.isFile() || !sourcePath(entry.name) || nonProductionSourceName(entry.name)) continue;
      if (files.length >= PHP_MAX_SOURCE_FILES) {
        limitations.add(`Stopped PHP source discovery at the ${PHP_MAX_SOURCE_FILES}-file bound.`);
        stopped = true;
        break;
      }
      const loaded = await readPhpFile(absolute, display, PHP_MAX_TOTAL_BYTES - totalBytes);
      if (loaded.limitation) limitations.add(loaded.limitation);
      if (loaded.totalExceeded) {
        limitations.add(`Stopped PHP source discovery at the ${PHP_MAX_TOTAL_BYTES}-byte project bound.`);
        stopped = true;
        break;
      }
      totalBytes += loaded.bytes ?? 0;
      if (loaded.document) files.push(loaded.document);
    }
  }
  await walk(absoluteTarget, 0);
  return { target: absoluteTarget, root: absoluteTarget, files, limitations: limitations.finish() };
}

export function resolvePhpProject(input: PhpProjectInput): Promise<PhpProject> {
  return typeof input === "string" ? loadUncached(input) : Promise.resolve(input);
}

export function createCachedPhpProjectLoader(target: string): () => Promise<PhpProject> {
  let cached: Promise<PhpProject> | undefined;
  return () => cached ??= loadUncached(target);
}
