{
  "rules": {
    ".read": true,
    ".write": "auth != null",
    "notes": "\".write\": true",
    "profiles": {
      "$uid": {
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
