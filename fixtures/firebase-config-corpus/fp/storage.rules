rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    /* allow write; */
    match /public/{path=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == resource.metadata.owner;
    }
  }
}
