interface:
  display_name: "CodeInspectus Threat Model"
  short_description: "Review findings with untrusted project context"
  default_prompt: "Use $codeinspectus-threat-model to add bounded threat-model context to an exact CodeInspectus scan without changing its raw findings."
policy:
  allow_implicit_invocation: false
